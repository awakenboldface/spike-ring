apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: helloweb
  labels:
    app: hello
spec:
  template:
    metadata:
      labels:
        app: hello
        tier: web
    spec:
      containers:
      - name: hello-app
        image: slander/spike-ring:v0.1.24
        ports:
        - containerPort: 8080
      - name: spike-python
        image: slander/spike-python:v0.1.24
        ports:
        - containerPort: 5000
